# CSharp-SQLServer-DAL-BLL-Code-Generator
C# Windows Forms tool that automatically generates clean Data Access Layer (DAL) and Business Logic Layer (BLL) code from SQL Server databases. Supports nullable columns, full CRUD operations, clean architecture, and reduces repetitive boilerplate code.
